import { TestBed } from '@angular/core/testing'; import { provideRouter, Router } from '@angular/router'; import { of, throwError } from 'rxjs'; import { RegisterComponent } from './register.component'; import { AuthService } from '../../../services/auth.service';
describe('RegisterComponent',()=>{let auth:jasmine.SpyObj<AuthService>;let router:Router;beforeEach(async()=>{auth=jasmine.createSpyObj('AuthService',['register']);await TestBed.configureTestingModule({imports:[RegisterComponent],providers:[provideRouter([]),{provide:AuthService,useValue:auth}]}).compileComponents();router=TestBed.inject(Router);spyOn(router,'navigate');});it('validates required fields and reports API failures',()=>{const f=TestBed.createComponent(RegisterComponent);const c=f.componentInstance;expect(c.registerForm.invalid).toBeTrue();c.registerForm.patchValue({nombre:'Ana',email:'ana@test.es',password:'123456'});auth.register.and.returnValue(throwError(()=>({error:{error:'Duplicado'}})));c.onSubmit();expect(c.errorMessage).toBe('Duplicado');});it('submits a valid registration',()=>{const f=TestBed.createComponent(RegisterComponent);const c=f.componentInstance;c.registerForm.patchValue({nombre:'Ana',email:'ana@test.es',password:'123456'});auth.register.and.returnValue(of({}));c.onSubmit();expect(auth.register).toHaveBeenCalled();expect(c.successMessage).toContain('Cuenta creada');});});
